<Window x:Class="WpfApp_08_06.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_08_06"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Enter Word:" Grid.Row="0"/>
        <TextBox x:Name="wordTextBox" Grid.Row="0" Margin="100,0,0,0"/>

        <Label Content="File Path:" Grid.Row="1"/>
        <TextBox x:Name="filePathTextBox" Grid.Row="1" Margin="100,0,0,0"/>

        <Button Content="Search" Click="SearchButton_Click" Grid.Row="2" HorizontalAlignment="Center" Margin="0,10,0,10"/>

        <TextBlock x:Name="resultTextBlock" Grid.Row="3" HorizontalAlignment="Center"/>
    </Grid>
</Window>
