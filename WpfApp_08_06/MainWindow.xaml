<Window x:Class="WpfApp_08_06.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_08_06"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Source Directory:" Grid.Row="0"/>
        <TextBox x:Name="sourceTextBox" Grid.Row="0" Margin="100,0,0,0" Width="300"/>

        <Label Content="Destination Directory:" Grid.Row="1"/>
        <TextBox x:Name="destinationTextBox" Grid.Row="1" Margin="100,0,0,0" Width="300"/>

        <Label Content="Number of Threads:" Grid.Row="2"/>
        <TextBox x:Name="threadsTextBox" Grid.Row="2" Margin="100,0,0,0" Width="50"/>

        <ProgressBar x:Name="progressBar" Grid.Row="3" Margin="100,10,100,10"/>

        <Button Content="Start" Click="StartButton_Click" Grid.Row="4" Margin="100,10,100,10"/>
    </Grid>
</Window>
